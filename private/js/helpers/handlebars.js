/*
var Showdown = require('showdown');
var showdown = new Showdown.converter();
Ember.Handlebars.helper('format-markdown', function(md) {
  if(md == undefined) {
    return "";
  }
  return new Handlebars.SafeString(showdown.makeHtml(md));
});
*/
